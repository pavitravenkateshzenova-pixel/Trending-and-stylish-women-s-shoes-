 9901342008="width=device-width, initial-scale=1.0">
<title>Teddy Shoes - Cute & Comfy Footwear</ women's fancy teddy shoes I>
<teddy bear brand>
/* ===== CSS START LINE 10 ===== */
:root{
  --pink: #FFB6D9;
  --brown: #8B5E3C;
  --cream: #FFF8F0;
  --dark: #2C2C2C;
  --white: #ffffff;
  --gray: #f5f5f5;
}

*{
  margin:0; 
  padding:0; 
  box-sizing:border-box;
  font-family: 'Arial', sans-serif;
}

html{
  scroll-behavior: smooth;
}

body{
  background: var(--cream);
  color: var(--dark);
  line-height: 1.7;
}

header{
  background: var(--white);
  padding: 18px 10%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: 0 3px 20px rgba(0,0,0,0.08);
  position: sticky;
  top:0;
  z-index: 999;
}

.logo{
  font-size: 30px;
  font-weight: 900;
  color: var(--brown);
  letter-spacing: 1px;
}
.logo span{ 
  color: var(--pink); 
}

nav{
  display: flex;
  gap: 30px;
}
nav a{
  text-decoration: none;
  color: var(--dark);
  font-weight: 600;
  font-size: 16px;
  transition: 0.3s;
}
nav a:hover{ 
  color: var(--pink); 
  border-bottom: 2px solid var(--pink);
}

.cart-btn{
  background: var(--pink);
  border: none;
  padding: 12px 24px;
  border-radius: 30px;
  cursor: pointer;
  font-weight: 700;
  font-size: 15px;
  transition: 0.3s;
}
.cart-btn:hover{
  background: var(--brown);
  color: white;
  transform: scale(1.05);
}

/* HERO */
.hero{
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 100px 10%;
  background: linear-gradient(135deg, var(--pink) 0%, #FFE0EC 100%);
  min-height: 85vh;
}
.hero-text h1{
  font-size: 56px;
  color: var(--brown);
  margin-bottom: 20px;
  line-height: 1.2;
}
.hero-text p{
  font-size: 19px;
  margin-bottom: 35px;
  max-width: 520px;
  color: #444;
}
.btn{
  background: var(--brown);
  color: white;
  padding: 16px 40px;
  border: none;
  border-radius: 35px;
  cursor: pointer;
  font-size: 18px;
  font-weight: 700;
  transition: 0.4s;
}
.btn:hover{
  background: var(--white);
  color: var(--brown);
  box-shadow: 0 5px 20px rgba(0,0,0,0.2);
}
.hero-img img{
  width: 480px;
  border-radius: 30px;
  box-shadow: 0 15px 40px rgba(0,0,0,0.25);
}

/* PRODUCTS */
.products{
  padding: 100px 10%;
  text-align: center;
  background: var(--white);
}
.products h2{
  font-size: 42px;
  margin-bottom: 15px;
  color: var(--brown);
}
.subtitle{
  margin-bottom: 60px;
  color: #666;
  font-size: 17px;
}
.product-grid{
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 40px;
}
.card{
  background: var(--gray);
  padding: 25px;
  border-radius: 25px;
  box-shadow: 0 6